apiVersion: v1
kind: Service
metadata:
  name: whack-a-pod
  namespace: whack-a-pod
  labels:
    app: whack-a-pod
spec:
  type: LoadBalancer
  selector:
    app: whack-a-pod
  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP
